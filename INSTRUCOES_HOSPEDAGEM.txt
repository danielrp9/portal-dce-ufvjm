# INSTRUÇÕES PARA HOSPEDAGEM - PORTAL DCE UFVJM

Este documento descreve os requisitos e passos necessários para colocar o sistema em produção no servidor da universidade.

## 1. Requisitos de Ambiente
O sistema foi desenvolvido em Python (Django) e requer um banco de dados **PostgreSQL**.

### Variáveis de Ambiente Necessárias:
Para segurança e flexibilidade, o sistema lê as configurações sensíveis do ambiente. Devem ser configuradas as seguintes variáveis:

*   `NEXT_PUBLIC_API_URL`: URL base da API (ex: `https://api.dce.ufvjm.edu.br`).
*   `NEXT_PUBLIC_SITE_URL`: URL base do site frontend (ex: `https://dce.ufvjm.edu.br`).
*   `DATABASE_URL`: String de conexão com o PostgreSQL.
    *   Formato: `postgres://usuario:senha@host:porta/nome_do_banco`
*   `SECRET_KEY`: Uma chave aleatória e longa para segurança do Django.
*   `DEBUG`: Definir como `False` em produção.
*   `ALLOWED_HOSTS`: Lista de domínios permitidos (ex: `dce.ufvjm.edu.br, api.dce.ufvjm.edu.br`).
*   `FRONTEND_URL`: URL onde o frontend estará rodando (necessário para o CORS).

## 2. Passos para Inicialização do Backend (Pasta /backend)

1.  **Ambiente Virtual:** Criar e ativar um novo venv.
    ```bash
    python -m venv venv
    source venv/bin/activate  # Linux/Mac
    ```
2.  **Instalação:** Instalar as dependências.
    ```bash
    pip install -r requirements.txt
    ```
3.  **Migrações:** Criar a estrutura do banco de dados no PostgreSQL.
    ```bash
    python manage.py migrate
    ```
4.  **Arquivos Estáticos:** Coletar arquivos para o servidor web.
    ```bash
    python manage.py collectstatic
    ```
5.  **Usuário Admin:** Criar o primeiro acesso ao painel administrativo.
    ```bash
    python manage.py createsuperuser
    ```

## 4. Passos para Inicialização do Frontend (Pasta /frontend)

1.  **Instalação:** `npm install`
2.  **Build:** `npm run build`
3.  **Execução:** `npm start`
    *   O frontend rodará como um servidor Node.js, permitindo SSR e atualizações em tempo real.

## 5. Notas Importantes para o Desenvolvedor
*   **Não enviar:** As pastas `venv/`, `node_modules/`, `.next/` e o arquivo `db.sqlite3`.
*   **Migrações Futuras:** Sempre que você alterar um arquivo `models.py` localmente, rode `python manage.py makemigrations`. Os arquivos gerados na pasta `migrations/` **DEVEM** ser enviados ao servidor para que o comando `migrate` lá funcione.
*   **CORS:** Se o frontend e o backend estiverem em domínios diferentes, certifique-se de que a `FRONTEND_URL` esteja configurada corretamente para permitir o acesso à API.

---
Documento gerado para auxílio na implantação do Portal DCE UFVJM.
